---
title: "v1: not_found · HTTP 404"
description: "No such resource for this account - an unknown job id, or a job that belongs to someone else."
---

<Warning>Legacy v1 reference. Use [v2 documentation](/quickstart) for new integrations. Shared guides describe the current API.</Warning>

<a id="sec-example-response" />

## Example response

```json
{
  "ok": false,
  "request_id": "req_…",
  "error": {
    "code": "invalid_request",
    "message": "No such resource for this account - an unknown job id, or a job that belongs to someone else."
  }
}
```

v1 failures carry `error.code` and `error.message` (plus `issues` on validation failures); transcript failures repeat the code as a top-level `reason` and add an `ai_fallback` hint. **v1 never emits this code**; it answers `invalid_request` for this situation.

<a id="sec-what-it-means" />

## What it means

No such resource for this account - an unknown job id, or a job that belongs to someone else.

<a id="sec-why-it-happens" />

## Why it happens

- The job id does not exist, or belongs to another account (both answer the same way, so ids cannot be enumerated).
- The id was truncated or taken from a different environment.

<a id="sec-what-to-do" />

## What to do

- Use the `job_id` exactly as the 202 response or the batch entry returned it.
- Poll with the same API key that created the job.

**Retryable?** No; the same id will keep answering 404. Every error response carries a `request_id`; include it when contacting support.

<a id="sec-related-errors" />

## Related errors

- [unauthorized](/v1/errors/unauthorized) (1101), a key that is wrong altogether fails before the lookup
