---
title: "v1: audio_ineligible · HTTP 422"
description: "Audio transcription cannot run on this input, so mode: \"audio\" (or ai_fallback) has nothing to work on."
---

<Warning>Legacy v1 reference. Use [v2 documentation](/quickstart) for new integrations. Shared guides describe the current API.</Warning>

<a id="sec-example-response" />

## Example response

```json
{
  "ok": false,
  "request_id": "req_…",
  "error": {
    "code": "unsupported_platform",
    "message": "Audio transcription cannot run on this input, so mode: \"audio\" (or ai_fallback) has nothing to work on."
  }
}
```

v1 failures carry `error.code` and `error.message` (plus `issues` on validation failures); transcript failures repeat the code as a top-level `reason` and add an `ai_fallback` hint. **v1 never emits this code**; it answers `unsupported_platform` for this situation.

<a id="sec-what-it-means" />

## What it means

Audio transcription cannot run on this input, so mode: "audio" (or ai_fallback) has nothing to work on.

<a id="sec-why-it-happens" />

## Why it happens

- `mode: "audio"` (or `ai_fallback: true`) on an input audio transcription cannot serve: an arbitrary page URL rather than a video or media file.

<a id="sec-what-to-do" />

## What to do

- Send the media's own URL or id.
- Drop `mode` to let the default path decide; captions are attempted first.

**Retryable?** No, not for this input. Every error response carries a `request_id`; include it when contacting support.

<a id="sec-related-errors" />

## Related errors

- [unsupported_platform](/v1/errors/unsupported_platform) (3001), the same input fails there without an audio mode
- [no_captions](/v1/errors/no_captions) (4103), the failure that usually precedes an audio retry
